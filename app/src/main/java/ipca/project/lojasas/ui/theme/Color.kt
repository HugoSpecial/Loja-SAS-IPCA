package ipca.project.lojasas.ui.theme

import androidx.compose.ui.graphics.Color

// --- Cores da Marca ---
val GreenPrimary = Color(0xFF00864F)
val RedPrimary = Color(0xFFEF2020)

// --- Cores de Fundo (Light) ---
val MyBackColor = Color(0xFFF5F5F5)

// --- Cores de Fundo (Dark) ---
val DarkBackground = Color(0xFF1C1B1F) // Fundo principal da aplicação (quase preto)
val DarkSurface = Color(0xFF2C2C2C)     // Fundo dos Cards/Inputs (um pouco mais claro)

// --- Cores Base ---
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val GrayText = Color(0xFF757575)